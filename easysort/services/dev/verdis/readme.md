3 services:

1. detect the type of waste on the belt
    1.1: 
2. detect if the belt is moving or not
    2.1: Crop images -> detect change in image pixels -> infer if the belt is moving or not

- Be able to validate the classifications
- From those two classifications infer what the correct classification is

3. count the number of "baller" produced daily